操作者 login scenario 验证 跑 the 服务 was 推出 following the 表现 request by the actor 操作者 the access to the 屏幕 on the management of the system ( for 例 guilogin ) 输入 login and 密码 and 查 the 功能 the 验证 then passes 控制 to the 例 of guiloginhandler which delegates to the 例 of operatoremanager the task of managing the 后者 交易 with 验证 the 数据 进入 by 运用 the panel the successful 操作者 is 通知 of a 落后 up the 应用 of which 要求 guiloginhandler to the 例 of guiprincipalehandler to 启用 access to 患者 applicant and that 目的 shall display the 主要 form ( for 例 guiprincipale ) this feature is was 描述 by the 序列 图 of 图 3 2