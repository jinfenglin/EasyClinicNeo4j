record 访问 this 特征 is has been 描述 by 合作 图 of 图 3 21 the main scenario : the 服务 is 开始 以下 the 明确的 请求 the actor part of the operator 访问 to the 面具 main ( for instance guiprincipale ) and there select the function the 管理 访问 then 通行证 control to the instance of guiprincipalehandler which delegates to the instance of guivisitahandler the 任务 of 提供 the 服务 the first 步 is to obtain a 名单 of all 访问 in 门诊病人 因此 control 通行证 to the instance of visitemanager through an iterative 处理 are the goods 恢复 of all 访问 and 一切 步 is 制作 in as follows : the panel 发现 through visitemanager the 身体 of 数据 on 访问 to a 访问 and then 通行证 control to the instance of that esamimanager 提供 through 实例 of examination for finding all examinations on the same 访问 this 名单 is passed guivisitahandler to the instance of which shall display the 访问 制作 on the 面具 on the 管理 访问 ( for instance guivisite ) the operator select the function visiting 放置 插 数据 访问 the new and 确认 control 通行证 to the application guivisitehandler of the 代表团 which is to 输入 the new 访问 to the instance of visitemanager 后者 through 访问 the panel and 确保 the 包容 of rescue 数据 on a new 游览 and 返回 control application guivisitahandler of which in turn is 主管 to 通知 成功 by viewing a 信息 in the form of 管理 访问 ( for instance guivisita )