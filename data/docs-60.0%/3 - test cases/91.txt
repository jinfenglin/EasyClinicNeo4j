test case 保留 a 访问 日期 : c41 control 之前 by 20/06/2003 another 访问 check 版 : 0 02 000 use case meets the 请求 for a 保留 你预测 服务 by outpatient a 患者 media priorities set up the 持续 保留 is ` : 06/10/2003 小时 12 00 访问 control 描述 test 输入 类型 选 访问 : 访问 check the 神谕 system 使 the 保留 of 访问 classes 盖 valid : ce3 ce8 classes are not valid : 没有