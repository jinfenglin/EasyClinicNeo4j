保留 for a visit after initial the service was launched following the express request by the actor the operator logs on to form patient 's main 部分 ( for instance guipaziente ) and there check the function of 保留 a visit 下一个 the first control and then passes to the instance of guipazientehandler which delegates to the instance of guiprenotavisitahandler the task of managing the service 选择 the 类型 of visit the application of class guiprenotavisitahandler displays the mask on the 保留 to visit the patient must be subject ( for instance guiprenotavisita ) on this operator select the form and confirms the 类型 of access and control returns to the instance of which guiprenotavisitahandler delegating the task to insert a new one application of prenotazionimanager creating the new 保留 the application of class through the application prenotazionimanager 保留 creates a new 保留 , at this point the body of 保留 shall verify the validity of 保留 or check that the new 保留 respects the sequence of 保留 valid if the reservations is a valid 保留 shall notify the success of the creation of a 保留 backward up to the instance of guiprenotavisitahandler finding data 可得到 the application of the proxy class guiprenotavisitahandler to the instance of prenotazionimanager task of finding the first date 可得到 when the visit can `` keep this date is guiprenotavisitahandler given to the instance of which shall to display it on form 保留 a visit ( for instance guiprenotavisita ) the operator confirmed the date proposed by the system and control is returned to the instance of guiprenotavisitahandler saving 保留 the application of class guiprenotavisitahandler at this point proceed to store the 保留 by delegating this prenotazionimanager task to the instance of this application through the application for 保留 shall saving data on the 保留 once this operation control passes to the instance of guiprenotavisitahandler which in turn deals with notify success by viewing a special message on the screen on the 保留 a visit ( for instance guiprenotavisita ) this feature is was described by the collaboration diagram of fig 3 17 also because ? the book a outpatient service is `the heart of it `is 思想 to 评论 on that functionality with un'activity diagram ( fig 3 18 ) in order to 使 more clear the iterations that we are among the 玩家 and the system itself