operator login scenario validation run the service was launched following the express request by the actor operator the access to the screen on the management of the system ( for 例 guilogin ) enter login and password and check the function the validation then 通行证 control to the 例 of guiloginhandler which delegates to the 例 of operatoremanager the task of managing the latter deals with validate the data entered by using the panel the successful operator is notified of a backward up the 应用 of which requires guiloginhandler to the 例 of guiprincipalehandler to enable access to patient applicant and that object 应 display the main form ( for 例 guiprincipale ) this feature is was described by the sequence diagram of fig 3 2