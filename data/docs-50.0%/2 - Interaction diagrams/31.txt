操作者 login 脚本 验证 run the service was launched 以下 the express request by the actor 操作者 the access to the 屏幕 on the management of the system ( for 例 guilogin ) 输入 login and password and check the 功能 the 验证 then passes 控制 to the 例 of guiloginhandler which delegates to the 例 of operatoremanager the 任务 of 管理的 the 后者 deals with 验证 the data entered by 运用 the panel the successful 操作者 is notified of a backward up the 应用 of which requires guiloginhandler to the 例 of guiprincipalehandler to enable access to 患者 申请人 and that 目的 应 display the 主要 形成 ( for 例 guiprincipale ) this 特征 is was described by the sequence 图 of 图 3 2