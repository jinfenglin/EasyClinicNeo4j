取消 reservation the 服务 was 推出 以下 the 表现 请求 by the actor the operator logs on to form on the main 部分 患者 ( for instance guipaziente ) and there 选择 the 消除 of a reservation the 控制 then 去 to the instance of the guipazientehandler which delegates to the instance of the guieliminaprenotazione处理程序 任务 of 管理的 the 服务 the 请求 for guieliminaprenotazione处理程序 delegation application prenotazionimanager is to obtain a list of all 预订 from 患者 this object through the 要求 for 预订 to 确保 retrieval of the list and 返回 to the instance of that guieliminaprenotazione处理程序 turn 提供 观看 数据 obtained in the 面具 ( for instance guieliminaprenotazione ) the operator selects from the list by the reservation 删除 and 控制 is 通过 to the instance of guieliminaprenotazione处理程序 which the 请求 for delegation prenotazionimanager of 完成 the transaction the application prenotazionimanager through to the application for reservation shall to verify that the 序列 is valid 甚至 after the 消除 of reservation only if so the 消除 of reservation is 完成 and will 返回 控制 guieliminaprenotazione处理程序 to the instance of which shall notify the success of the transaction with a 信息 on the screen ( for instance guieliminaprenotazione ) this feature is was described by the collaboration diagram 图 3 of 20