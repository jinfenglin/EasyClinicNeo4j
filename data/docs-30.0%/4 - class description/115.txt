class guiprincipale 日期 : 18/09/2003 version : 0 01 000 描述 使 the main form of easy clinic2003 框 tower this form is task of 规定 the patient in the choice of information to be 显示 属性 name 访问 描述 private btnanagrafica button used to 访问 the 个人 details 部分 private btncartellaclin button used to 访问 the ICA 部分 夹 clinic private btnprenotazioni button used to 访问 the reservations 部分 private btnuscita button used to leave application methods signature show ( ) public 访问 描述 view mask signature abilitaaccesso ( ) public 访问 描述 manages the 事件 generated when the procedure validation ends with the success patient is then enabled signature cartellaclinica ( ) public 访问 描述 manages the 事件 generated when the patient obtained 访问 点击 夹 clinic to 访问 your 夹 clinic signature datipaziente ( ) public 访问 描述 manages the 事件 generated when the patient obtained 访问 点击 the button data details to view its data details signature reservations ( ) public 访问 描述 manages the 事件 generated when the patient obtained 访问 点击 the button reservations to 看到 reservations which set