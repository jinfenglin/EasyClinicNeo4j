class 访问 date : 15/09/2003 version : 0 01 000 it contains data description of a 访问 保持 in outpatient 属性 name 访问 description history made history private doctor private data on where the patient has 支持的 the 访问 private 诊断 made by medical 诊断 private 原因 grounds 访问 private rilevazionistrume instrumental observations made ntal during the 访问 private therapy treatment prescribed by the doctor methods 签名 创建 ( ) 上市 访问 description its task is to 创建 a 新 object of 类型 访问 签名 delete ( ) 上市 访问 provides description of a 访问 to 签名 save ( ) 上市 访问 description provides data storage to a 访问 within the s i o 签名 getattributes ( ) 上市 访问 it description to find all the information on a 访问 签名 setattributes ( ) 上市 访问 it description to set the 属性 of a patient