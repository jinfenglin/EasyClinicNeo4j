class booking date : 15/09/2003 version : 0 01 000 it contains 数据 describing a reservation that is was performed in 门诊病人 attributes name 访问 description datarichiesta private 数据 that has been 携带的 out ` reservations datavisita private date of the 访问 will 采取 place private grounds reason for which it was the 请求 private 小时 now that the 访问 will 采取 place methods signature create ( ) 上市 访问 description its task is to create a new 目的 of type reservation signature delete ( ) 上市 访问 provides description to a reservation signature save ( ) 上市 访问 description provides the 存储 of a reservation in the s i o signature getattributes ( ) 上市 访问 it description of the 恢复 of all information relating to a reservation signature setattributes ( ) 上市 访问 it description to 组 the attributes of a reservations