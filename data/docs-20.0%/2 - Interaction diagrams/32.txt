login failed validation operator scenario the 服务 was 推出 following the explicit request by the actor operator the access to the screen on the management of the system ( for 例 guilogin ) enter login and 密码 and check the function the validation then passes control to the 例 of guiloginhandler which delegates to the 例 of operatoremanager the 任务 of managing the latter deals with validate the data entered by 运用 the panel the operator of the negative outcome is notified backward up to the 例 of which shall guiloginhandler to display an error message on mask guilogin this 特征 is was 描述 by sequence diagram of 图 3 3