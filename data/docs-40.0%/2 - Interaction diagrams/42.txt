保留 for a 访问 after initial the service was 推出 following the 表现 请求 by the actor the operator 日志 on to form patient 's 主要 部分 ( for instance guipaziente ) and there 查 the 功能 of 保留 a 访问 next the first control and then passes to the instance of guipazientehandler which 代表 to the instance of guiprenotavisitahandler the task of 管理的 the service 选择 the 类型 of 访问 the application of class guiprenotavisitahandler 显示器 the 面具 on the 保留 to 访问 the patient must be 学科 ( for instance guiprenotavisita ) on this operator 选择 the form and confirms the 类型 of 访问 and control returns to the instance of which guiprenotavisitahandler 委托 the task to insert a new one application of prenotazionimanager creating the new 保留 the application of class through the application prenotazionimanager 保留 creates a new 保留 , at this point the 身体 of 保留 shall verify the validity of 保留 or 查 that the new 保留 尊重 the sequence of 保留 有效 if the 预订 is a 有效 保留 shall 通知 the success of the creation of a 保留 落后 up to the instance of guiprenotavisitahandler finding data available the application of the 代理 class guiprenotavisitahandler to the instance of prenotazionimanager task of finding the first date available when the 访问 can `` keep this date is guiprenotavisitahandler given to the instance of which shall to display it on form 保留 a 访问 ( for instance guiprenotavisita ) the operator confirmed the date 建议 by the system and control is returned to the instance of guiprenotavisitahandler saving 保留 the application of class guiprenotavisitahandler at this point proceed to store the 保留 by 委托 this prenotazionimanager task to the instance of this application through the application for 保留 shall saving data on the 保留 once this operation control passes to the instance of guiprenotavisitahandler which in turn deals with 通知 success by 观看 a special 信息 on the screen on the 保留 a 访问 ( for instance guiprenotavisita ) this 特征 is was 描述 by the collaboration 图 of 图 3 17 也 because ? the book a outpatient service is `the heart of it `is thought to comment on that 功能 with un'activity 图 ( 图 3 18 ) in 订购 to make more clear the iterations that we are 其中 the players and the system itself