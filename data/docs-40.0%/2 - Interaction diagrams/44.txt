cancel 保留 the 服务 was launched 以下 the 表现 request by the 演员 the 操作者 logs on to form on the 主要 section patient ( for instance guipaziente ) and there select the elimination of a 保留 the control then goes to the instance of the guipazientehandler which delegates to the instance of the guieliminaprenotazione处理程序 任务 of managing the 服务 the request for guieliminaprenotazionehandler delegation application prenotazionimanager is to 获得 a list of all reservations from patient this object through the requests for reservations to 确保 retrieval of the list and 返回 to the instance of that guieliminaprenotazionehandler 转 提供 viewing data 获得 in the mask ( for instance guieliminaprenotazione ) the 操作者 selects from the list by the 保留 delete and control is passed to the instance of guieliminaprenotazionehandler which the request for delegation prenotazionimanager of completing the transaction the application prenotazionimanager through to the application for 保留 shall to 校验 that the 序列 is valid 甚至 after the elimination of 保留 only if so the elimination of 保留 is completed and will 返回 control guieliminaprenotazionehandler to the instance of which shall 通知 the success of the transaction with a 信息 on the screen ( for instance guieliminaprenotazione ) this 特征 is was 描述 by the collaboration diagram 图 3 of 20