操作者 login 脚本 validation run the 服务 was 推出 following the express request by the 演员 操作者 the 访问 to the screen on the 管理 of the 系统 ( for 例 guilogin ) 输入 login and password and 查 the 功能 the validation then 通行证 control to the 例 of guiloginhandler which delegates to the 例 of operatoremanager the task of 管理的 the latter deals with 验证 the data entered by using the 面板 the 成功 操作者 is notified of a 落后 up the application of which requires guiloginhandler to the 例 of guiprincipalehandler to enable 访问 to 患者 applicant and that object shall display the main form ( for 例 guiprincipale ) this 特征 is was described by the 序列 diagram of fig 3 2