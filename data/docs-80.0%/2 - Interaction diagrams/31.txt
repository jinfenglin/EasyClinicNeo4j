operator 登录 脚本 验证 run the 服务 was 推出 以下 the 表现 request by the 演员 operator the 访问 to the 屏幕 on the 管理 of the 系统 ( for 例 guilogin ) 输入 登录 and 密码 and 查 the 功能 the 验证 then passes 控制 to the 例 of guiloginhandler which 代表 to the 例 of operatoremanager the 任务 of 管理的 the 后者 deals with validate the 数据 进入 by 运用 the 面板 the 成功 operator is 通知 of a 落后 up the 应用 of which requires guiloginhandler to the 例 of guiprincipalehandler to enable 访问 to patient 申请人 and that 目的 应 显示 the 主要 形成 ( for 例 guiprincipale ) this 特征 is was 描述 by the 序列 diagram of fig 3 2