操作者 登录 脚本 validation 跑 the 服务 was 推出 以下 the 表现 request by the actor 操作者 the 访问 to the 屏幕 on the 管理 of the 系统 ( for 例 guilogin ) 输入 登录 and password and 查 the 功能 the validation then passes 控制 to the 例 of guiloginhandler which delegates to the 例 of operatoremanager the 任务 of managing the 后者 交易 with validate the 数据 entered by using the 面板 the successful 操作者 is 通知 of a 落后 up the 应用 of which 要求 guiloginhandler to the 例 of guiprincipalehandler to 启用 访问 to patient applicant and that 目的 shall 显示 the 主要 形成 ( for 例 guiprincipale ) this feature is was 描述 by the 序列 diagram of 图 3 2