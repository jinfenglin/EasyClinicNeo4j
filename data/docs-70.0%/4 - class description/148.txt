类 prenotazionimanager 日期 : 16/09/2003 版 : 0 01 000 description has the 任务 to manage 操作 要求 访问 to 数据 有关 to reservations 属性 name 访问 description 方法 签名 deleteprenotazione ( ) public 访问 description has the 任务 to manage the 处置 of a reservations 签名 editprenotazione ( ) public 访问 description its 任务 is to manage the 更改 数据 旅馆 评论 签名 getdatadisponibile ( ) public 访问 description 特定 the 性质 of the 访问 calculates the 第一 日期 可得到 when the 访问 can `` 保持 签名 getprenotazioneat ( ) public 访问 description its 任务 is to find all the 信息 on a 保留 签名 getprenotazionipaziente ( ) public 访问 description its 任务 is to find all reservations a patient 签名 newprenotazione ( ) public 访问 description its 任务 is to manage the 包容 of a 新 保留 签名 saveprenotazione ( ) public 访问 description its 任务 is to 保存 a standing a 保留 签名 钢琴settimanale ( ) public 访问 description its 任务 is to find all the 信息 必要 for the settlement 计划 reservations weekly 签名 温度鸣叫 ( ) public 访问 description its 任务 is to 计算 the average 时间 of 有待 that is `to 服务 occurred 请求 an outpatient service