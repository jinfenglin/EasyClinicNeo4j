class guiprincipale 日期 : 18/09/2003 版 : 0 01 000 description 使 the main 形成 of 简单 clinic2003 框 塔 this 形成 is 任务 of 规定 the patient in the 选择 of 信息 to be displayed attributes 那么 访问 description private btnanagrafica 按键 用过的 to 访问 the anagrafica section private btncartellaclin 按键 用过的 to 访问 the ICA section 夹 诊所 private btnprenotazioni 按键 用过的 to 访问 the reservations section private btnuscita 按键 用过的 to leave 应用 方法 那么 show ( ) 上市 访问 description 视图 面具 signature abilitaaccesso ( ) 上市 访问 description manages the 事件 产生 when the 程序 validation goes 好 signature 实验室 ( ) 上市 访问 description manages the 事件 产生 when the 操作者 访问 to the section devoted to the 实验室 signature 管理 ( ) 上市 访问 description manages the 事件 产生 when the 操作者 访问 to the section on the 管理 of the 门诊病人 signature 医生 ( ) 上市 访问 description manages the 事件 产生 when the 操作者 访问 to the section on 管理 数据 on 医生 who are 门诊病人 signature primavisita ( ) 上市 访问 description manages the 事件 产生 when the 操作者 开始 the 程序 of 保留 a 第一 访问 signature 游 ( ) 上市 访问 description manages the 事件 产生 when the 操作者 访问 to the section on 管理 访问 signature reservations ( ) 上市 访问 description manages the 事件 产生 when the 操作者 reservations 访问 to the section to have a report them