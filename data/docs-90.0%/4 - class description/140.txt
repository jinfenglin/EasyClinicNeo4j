类 guivisita 日期 : 16/09/2003 版 : 0 01 000 描述 使 the mask 用过的 for 管理的 of 访问 中 of the 门诊病人 attributes 那么 访问 描述 private btndelete 按键 that 允许 you to 开始 程序 for 删除 of a 访问 private btnedit 按键 that 允许 you to 开始 程序 for changing a 访问 private btnnew 按键 that 允许 you to 开始 程序 for 输入 a new 访问 private btnsave 按键 that 允许 you to 开始 程序 to 保存 a 访问 just 变 or 添加 private btnundo 按键 that 允许 you to 取消 the 持续 手术 执行 方法 签名 notificasuccesso ( ) 上市 访问 描述 its 任务 is to 通知 the 成功 of 签名 visualizzaelencovisite ( ) 上市 访问 描述 its 任务 is to 看到 the 名单 of 访问 签名 考试 ( ) 上市 访问 描述 its 任务 is to 管理 the 事件 产生 when the 操作者 选择 a 访问 from the 名单 and 要求 访问 to the section that 节目 the 检查 to such 访问 签名 访问 ( ) 上市 访问 描述 its 任务 is to 找 the 信息 必要 for the 实现 of guivisita